// Shared React hooks
// Reusable hooks that can be used across features

export { useDataCache, usePrefetchCache } from './use-data-cache';
export { useRoutePreloader, useNavigationPreloader } from './use-route-preloader';
export { useInstantProducts } from './use-instant-products';
export { useInstantDashboard } from './use-instant-dashboard';

// Example: export { useLocalStorage } from './useLocalStorage'
// Example: export { useDebounce } from './useDebounce'

// Hooks will be exported here as they are created
